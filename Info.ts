
import { GameInfoType } from './src/GameInfoType.ts';

const helpText = `

### {game title} Help

CenturyLink is not all that bad.

*But that's* just my opinion.

`

export const info: GameInfoType = {
    title: `This Cave is Trying to Kill You`,
    helpText,
    firstLocation: 'Intro',
}
